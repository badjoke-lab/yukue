{
  "name": "@badjoke-lab/yukue-observation-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && node --test test/*.test.mjs",
    "check": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@badjoke-lab/yukue-schemas": "workspace:*",
    "@badjoke-lab/yukue-validation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
