name: Audit Matsuri corpus quality

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/audit-matsuri-corpus-quality.yml"
      - "apps/matsuri/scripts/load-matsuri-dataset.mjs"
      - "apps/matsuri/src/data/**"
      - "data/public/matsuri/**"
      - "docs/nationwide-corpus-scaling.md"
      - "scripts/audit-matsuri-corpus-quality.mjs"
      - "package.json"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/audit-matsuri-corpus-quality.yml"
      - "apps/matsuri/scripts/load-matsuri-dataset.mjs"
      - "apps/matsuri/src/data/**"
      - "data/public/matsuri/**"
      - "docs/nationwide-corpus-scaling.md"
      - "scripts/audit-matsuri-corpus-quality.mjs"
      - "package.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 11.10.0

      - name: Install dependencies from lockfile
        run: pnpm install --frozen-lockfile

      - name: Build corpus quality report
        run: pnpm audit:matsuri:quality -- --output artifacts/matsuri-corpus-quality

      - name: Publish report in workflow summary
        if: always()
        run: |
          if [ -f artifacts/matsuri-corpus-quality/report.md ]; then
            cat artifacts/matsuri-corpus-quality/report.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Matsuri corpus quality report was not generated." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload corpus quality report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: matsuri-corpus-quality-${{ github.sha }}
          path: artifacts/matsuri-corpus-quality/
          if-no-files-found: error
          retention-days: 14
